<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base del sistema (claro). La UI real la controla Compose (Material 3). -->
    <style name="Theme.CalculadoraIA" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
